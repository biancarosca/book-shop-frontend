import React from 'react';
import styled from 'styled-components';

const WishlistPage = () => {
    return(
        <StyledWrapper>Wishlist</StyledWrapper>
    );
}

const StyledWrapper = styled.div`
    width: 100%;
    min-height: 90vh;
`

export default WishlistPage;