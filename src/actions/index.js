import {searchUpdate,loadBooks,restoreSearch} from './booksAction';
import {toggleActiveState} from './navAction';

const allActions ={
    loadBooks,
    searchUpdate,
    restoreSearch,
    toggleActiveState,
};

export default allActions;